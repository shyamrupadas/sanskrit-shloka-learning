{
  "name": "@sanskrit-shloka-learning/api-contract",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "contract:generate": "node scripts/generate-contract.mjs",
    "test": "node --test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
