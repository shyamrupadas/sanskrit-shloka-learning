# Тикет 03 — порядок и удаление советов

Дата: 2026-09-19. База реализации: `a38ba36134fb7a0782a0819abb42dc7d98bb0e15`, ветка `main`.

Код, локальные проверки и visual review завершены. **Владелец 2026-09-19 исключил production-выпуск и production smoke из этой задачи и поручил закрыть тикет после готовности остальной работы.** Тикет закрыт по этому решению. Новые API не развёрнуты этой задачей, флаг новой админки не включён; фактически выпущенных версий backend/frontend в рамках реализации нет.

## Реализация

- POST `/api/admin/learning/tips/{tipId}/move` принимает `direction: up | down`; DELETE `/api/admin/learning/tips/{tipId}` окончательно удаляет совет. Обе операции возвращают подтверждённый список, защищены административной ролью и описаны TypeSpec. OpenAPI, клиент и серверные типы регенерированы; генератор дополнен именованием DELETE-методов.
- Добавление, перестановка и удаление используют общую транзакционную блокировку `learning_tips`. Перестановка меняет позиции двух соседей одним UPDATE; чтение итогового списка входит в транзакцию. Сбой до commit откатывает изменения. Неизвестный ID возвращает 404, недопустимое направление/край списка — 400.
- Удаление использует существующий FK `ON DELETE CASCADE` для всех переводов; позиции оставшихся материалов не переписываются. Последний совет удаляется, общее чтение возвращает `items: []`. Seed остаётся одноразовой миграцией.
- UI обновляет только административный query cache и только подтверждённым ответом команды. На время записи повторные команды блокируются; незавершённое чтение отменяется. Ошибки порядка и удаления показаны раздельными сообщениями. Диалог называет совет, объясняет необратимость и позволяет отмену.
- Пользовательское чтение и сохранённая серия текущей попытки не изменялись; фоновой синхронизации не добавлено.
- Workflow frontend поддерживает ручной `workflow_dispatch` только для `main` и передаёт Repository Variable `VITE_ADMIN_LEARNING_ENABLED` в build. Отсутствующий флаг сохраняет прежний интерфейс; выставлять `true` можно только после проверки фактически развёрнутых API. Это ручной операционный gate, не автоматическое доказательство готовности backend.

## Проверки

Использованы согласованные seams: API-обработчики, repository/executor, generated contract, RTL/router и существующий browser smoke. Новые основные сценарии сначала падали до реализации.

| Проверка | Результат |
| --- | --- |
| `pnpm lint` | PASS |
| `pnpm typecheck` | PASS: API, contract, web |
| `pnpm --filter @sanskrit-shloka-learning/web build` | PASS |
| `pnpm test` | PASS: API 119, contract 11, web unit/integration 223, Playwright 14 |
| `VITE_ADMIN_LEARNING_ENABLED=true pnpm --filter @sanskrit-shloka-learning/web exec playwright test e2e/admin-tip-editing.test.ts` | PASS 1/1; в default suite этот сценарий пропущен из-за выключенного флага |

Targeted API: `node --import ./node_modules/tsx/dist/esm/index.mjs --test --test-name-pattern='learning tips' src/api/api-handlers.service.test.ts` — 4/4; `src/learning/postgres-learning-tip.repository.test.ts` — 5/5. Contract: `node --test openapi-contract.test.mjs` — 11/11. RTL: `vitest run src/app/admin-tip-editing.test.tsx` — 11/11. Команды запускались через `pnpm --filter` соответствующего workspace.

API защищает доступ, общее чтение после перестановки, append, удаление и пустой список. Fake executor проверяет блокировку, транзакционную видимость, rollback после записи и чтение других языковых версий. Это модель поведения, а не проверка реального PostgreSQL. Существующий `migration-runner.test.ts` защищает каскадный FK и отсутствие повторного seed. RTL покрывает оба направления, края, ожидающую/неуспешную команду, подтверждение/отмену и последний совет. Browser smoke проверяет reload после перестановки и удаления, native Back формы и сохранность маршрутов каталога. Существующий `e2e/learn-shloka.test.ts` повторно подтвердил неизменность снимка советов в текущей попытке.

Первый полный запуск выявил typography guardrail: стили текста кнопок были вынесены в строковую константу вне JSX. Стили перенесены на Button, цвет явно типизирован как CSS color; targeted guardrail и повторный полный запуск успешны. Sandbox блокировал localhost (`listen EPERM`); browser-проверки выполнены с разрешением вне sandbox.

## pen.dev references и visual evidence

Read-only launcher открыл сохранённый `design/pen-design.pen`; прочитаны актуальная schema, `Get` с `resolveInstances: true` и `resolveVariables: true`. Сессия завершилась с кодом 0, дизайн не изменялся.

| Состояние | Saved Pen reference | Evidence приложения (временный screenshot) |
| --- | --- | --- |
| Полный список | `ErT0Q` | `/tmp/tip-order-app-list.png` |
| Подтверждение удаления | `lQ9Jc`, modal `vtfzn` | `/tmp/tip-order-app-confirm.png` |
| Ошибка порядка | `d39xxc` | `/tmp/tip-order-app-move-error.png` |
| Ошибка удаления | `ihTkc` | `/tmp/tip-order-app-delete-error.png` |
| Последний совет | `IU3O3` | `/tmp/tip-order-app-last.png` |
| Пустой список | `k3DZo8` | `/tmp/tip-order-app-empty.png` |

Evidence получен через Playwright MCP на `http://127.0.0.1:4175/admin/learning`, с точными API mocks и включённым preview-флагом. Viewport совпадает с Pen: 390×844, подтверждение 390×900. Проверены композиция, размеры, spacing, typography, цвета, доступные/отключённые controls и состояния. Уточнены белый фон outline-кнопок и отступ 18 px пустого состояния. Visual review выполнил агент `/root`. Design exceptions нет. Временные exports, screenshots и автоматически созданный MCP log удалены после проверки; в репозиторий не включаются.

## Standards

Reviewer `/root/review_standards`: документированных нарушений и обоснованных baseline smells нет. Соблюдены frontend-границы, TypeSpec/Nest boundary и согласованные уровни тестирования. **0 замечаний к коду.**

## Spec

Reviewer `/root/review_spec`: пропущенных функциональных требований, неверной реализации или расширения scope не обнаружено. **0 замечаний к коду.** Операционный gate выпуска первоначально отмечен отдельно, затем передан владельцу вне scope этой задачи по его явному решению. Дополнительный diff workflow также проверен обоими reviewers без замечаний. Локальные mocks не выдаются за production-проверку.

## Завершение выпуска

Этот раздел — передача владельцу для самостоятельного выпуска. Владелец явно запретил production-выпуск в рамках задачи. Push, production-действия и смена Repository Variables не выполнялись.

1. Выпустить backend из коммита реализации через существующий backend CI/Amvera workflow. Зафиксировать source SHA, release SHA и фактическую версию Amvera. Дождаться readiness.
2. Проверить на развёрнутом API POST/PATCH советов, move и DELETE: 401 без сессии, 403 для обычного пользователя, успешный CRUD на специально созданном временном материале администратором; затем общий GET обычным пользователем. Проверить порядок и восстановить порядок исходных материалов после smoke. Не удалять реальные материалы для проверки последнего совета: этот случай защищён тестами, а live-проверку выполнять только в согласованной изолированной среде.
3. После успешного smoke установить Repository Variable `VITE_ADMIN_LEARNING_ENABLED=true`. Запустить `Application production release` вручную для `main`; зафиксировать source SHA и workflow run. До шага 2 флаг не включать.
4. На выпущенном frontend пройти «Еще → Админка → Обучение», проверить создание/редактирование, порядок и удаление временного совета; проверить каталог и открытое заучивание. Зафиксировать фактические версии и результаты в отдельном отчёте выпуска. Реализация этого тикета уже закрыта по решению владельца.

**Новых DB migrations нет.** Для работы необходима миграция предыдущей спецификации `0011_learning_tips`; она должна быть применена до зависимого API. В этой задаче внешние миграции не применялись и их production-состояние не подтверждалось.
