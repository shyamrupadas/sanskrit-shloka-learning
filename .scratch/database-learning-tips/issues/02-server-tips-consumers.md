# 02 — Перевести страницу и серию советов на данные из БД

**What to build:** Пользователь читает серверные советы на отдельной странице и во время заучивания; начатая серия сохраняет список и позицию при возврате и перезагрузке, а новые входы получают актуальное содержимое.

**Blocked by:** 01 — Подготовить UI-контракт серверных советов

**Status:** ready-for-agent

- [ ] Совет хранится отдельно от языковых версий, имеет стабильный id и общий порядок; пара «совет — локаль» уникальна, осиротевших версий нет. Заголовок и текст обязательны с пределами 120/2000 символов.
- [ ] Авторизованный пользователь без административной роли получает полный упорядоченный список id/title/text на русском; локаль не передаётся и настройки учётной записи не расширяются. Пустой список — успешный ответ.
- [ ] Три исходных совета однократно перенесены без изменения текстов и порядка. Повторные развёртывания не затирают дальнейшие правки и не восстанавливают удаления.
- [ ] Страница «Советы» читает свежий список при каждом входе и перезагрузке, сохраняет раскрытие/сворачивание и не меняет содержимое фоновым обновлением во время чтения.
- [ ] Первое открытие диалога запрашивает список; первый успешный ответ фиксирует состав, порядок и тексты до конца попытки. Повторные открытия используют сохранённый список.
- [ ] Советы идут последовательно без повторов; после последнего переход недоступен и виден конец серии. Закрытие диалога позицию не меняет.
- [ ] Переход «Все советы» → назад и перезагрузка сохраняют незавершённую попытку, список и текущую позицию. Чтение отдельной страницы не продвигает серию.
- [ ] Изменение, добавление, удаление и перестановка на сервере не меняют сохранённую серию. Отдельная страница может уже показывать новые тексты; новая попытка получает свежий список при первом открытии диалога.
- [ ] Завершение или отмена попытки очищают только её серию. Поздний ответ предыдущей попытки не заменяет состояние новой.
- [ ] Ошибка загрузки видна отдельно, допускает повтор и не фиксирует пустой список. Успешный пустой список фиксируется до конца попытки и показывает «Советов пока нет». Диалог можно закрыть и продолжить заучивание.
- [ ] При старом формате с одним индексом однократно перезапускается только серия советов; попытка и пользовательский прогресс сохраняются. Повтор прежнего совета при переходе допустим.
- [ ] После переключения обоих потребителей встроенный массив не используется как источник или fallback; серверного хранения серии, синхронизации устройств и фоновых подписок нет.
- [ ] TypeSpec, generated artifacts и реализация согласованы; проверки API чтения, миграции, contract, UI и browser-сохранения проходят.
- [ ] До выпуска frontend применены миграции и проверены фактически развёрнутый API и начальные данные. Backend выпускается аддитивно, пока старый клиент продолжает работать.
- [ ] Оба потребителя, сохранение серии и переход старого формата включены одним выпуском frontend после готовности API и дизайна. Работоспособность каталога и заучивания сохранена.
- [ ] Визуальная проверка приложения выполнена по актуальному сохранённому контракту; в отчёте перечислены references, evidence, автор review и миграции, которые необходимо применить.

## Parent

[Советы из БД](../spec.md).

## Проверки

Переиспользовать integration tests страницы обучения и потока заучивания с точным mocked API; проверить список/ошибку/пустой ответ, неизменность серии и новый вход. Playwright — возврат через native Back, reload и переход старого формата. API-обработчики с fake repositories — доступ, ru, порядок; migration/executor checks — однократное наполнение; contract tests — generated surface. Выполнить targeted проверки и обязательные lint, typecheck и frontend unit tests, а также затронутые API/contract checks. Live DB в обычные тесты не добавлять.

## pen.dev references

[Сохранённый UI-контракт и результаты проверки](../design-review.md) подготовлены и приняты в задаче 01. Коллизия отсутствия дизайна закрыта. Перед frontend-реализацией перечитать релевантные сохранённые узлы по pen-ui-contract. Общая адаптивная оболочка `ModalContent` уже реализована в задаче 01; получение данных и состояния потребителей остаются в этой задаче.
